**Last Updated: June 13th 2025**
```markdown
# Client, Organization, and ClientOrganization Models & Admin Interfaces Enhancements

## Admin Interface Improvements:
- **Form Validations**: Added form validations to prevent future dates in `from_date`, `to_date`, and `current_org_joining_day`.
- **Auto-population of Location Fields**: Auto-populated location fields (`current_industry`, `current_country`, `current_state`, `current_region`) based on the associated `current_organization` and `current_org_city`.
- **Custom Filter (IsActiveFilter)**: Introduced a custom filter for easy filtering of clients by active/inactive status.
- **Soft Delete and Restore**: Enabled soft delete and restore functionality for clients.
- **Headquarter City Validation**: Enhanced form validation to ensure the `headquarter_city` is part of the city list in the `OrganizationAdminForm`.
- **Mandatory Fields**: Made certain fields mandatory (e.g., `industry` for `OrganizationAdmin` and `global_region` for `IndustryAdmin`).

## Model Enhancements:
- **Industry Model**: Added a `global_region` field, enforcing a unique constraint on `name` and `global_region`.
- **Organization Model**: Linked `headquarter_city` to the `City` model and added city validation.
- **ClientOrganization Model**: Added fields like `from_date`, `to_date`, and `corporate_affiliation_city`. Ensured consistent date validation and postal code checks.
- **Client Model**: Introduced new fields (`current_region`, `current_country`, `current_state`, etc.) to represent the working environment and improve data integrity. Enhanced `clean()` and `save()` methods to validate and support soft deletion.

## Serializers:
- **Validation**: Ensured better validation across various serializers, including length and consistency checks for `industry`, `organization name`, `client email`, `phone number`, and `postal codes`.

## ViewSets & Pagination:
- **Filtering, Search, and Ordering**: Enhanced filtering, search, and ordering capabilities for `Client`, `Organization`, and `ClientOrganization` using `BulkModelViewSet` and custom filters.

---

# Soft Delete Implementation with TimeStampedModel and ActiveManager

## Soft Delete Functionality:
- Introduced soft delete functionality using `TimeStampedModel` and implemented `soft_delete` and `restore` methods for records.
- Created an `ActiveManager` to filter out soft-deleted records by default and added `is_active` field for record status management.

---

# Invoice, InvoicePayment, and PaymentMethod Admin Interfaces

## Invoice and Payment Method Admin Enhancements:
- **List View Enhancements**: Enhanced list views with relevant fields (e.g., `is_active`, `is_default`).
- **Search and Filter**: Introduced search and filter functionalities for fields like `name`, `code`, and `is_active`.

## Invoice Admin Interface:
- **OverdueFilter**: Added an `OverdueFilter` to check overdue invoices based on payment status and due dates.
- **Soft Deletion**: Introduced soft deletion for invoices and optimized database queries using `select_related`.
- **Refined List View**: Refined list view with key fields like `invoice_number`, `amount`, `status`, and `remaining_amount`.
- **InvoicePayment Inline**: Integrated `InvoicePaymentInline` for related payments, optimized with `StandardResultsSetPagination`.

## InvoicePayment Admin:
- Enhanced search, filter, and pagination capabilities, and allowed autocomplete for `method` and `invoice` fields.

---

# Invoice, InvoicePayment, and PaymentMethod Models with Validation and Soft Delete

## Models:
- Created models for `InvoiceStatus`, `PaymentMethod`, `Invoice`, and `InvoicePayment` with essential fields and validation for consistency.
- Incorporated soft delete functionality in both `Invoice` and `InvoicePayment`.
- Optimized database with indexes on `due_date` and `deleted_at` for faster querying.

## Managers:
- Utilized `ActiveManager` for filtering soft-deleted records and added a custom manager for handling `is_active` flags.

---

# Serializers for Invoice with Read, Write, and Base Logic

## InvoiceBaseSerializer:
- Implemented a base serializer to include common invoice fields and validation.

## InvoiceReadSerializer:
- Extended the base serializer to include read-only fields like `status_display`, `payment_method_display`, and `is_overdue`.

## InvoiceWriteSerializer:
- Added validation for `due_date`, ensuring it is not earlier than `issue_date`.

---

# Invoice ViewSet Enhancements with Soft Deletion and Custom Actions

- Implemented custom soft deletion logic in `destroy()` method and added a custom permission (`IsNotSoftDeleted`) to exclude soft-deleted records.
- Enhanced filtering for invoices by `status_code`, `client_id`, and other fields.
- Added custom actions (`mark_paid`, `cancel`) to update invoice status based on user input.

---

# Location Models with Enhanced Postal Code Validation

## Models:
- Introduced `GlobalRegion`, `Country`, `State`, and `City` models for better location tracking.
- Added postal code validation for multiple countries and enforced consistency between `state` and `country` fields.

## Admin Interface:
- Improved location forms with dynamic filtering based on selected countries.
- Integrated soft delete functionality for location models and ensured data integrity with enhanced validation.
```

